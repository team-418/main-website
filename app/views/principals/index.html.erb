<p id="notice"><%= notice %></p>

<h1>Listing Principals</h1>

<table>
  <thead>
    <tr>
      <th>Created at</th>
      <th>Last updated at</th>
      <th>User</th>
      <th>Institution</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @principals.each do |principal| %>
      <tr>
        <td><%= principal.created_at %></td>
        <td><%= principal.last_updated_at %></td>
        <td><%= principal.user %></td>
        <td><%= principal.institution %></td>
        <td><%= link_to 'Show', principal %></td>
        <td><%= link_to 'Edit', edit_principal_path(principal) %></td>
        <td><%= link_to 'Destroy', principal, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Principal', new_principal_path %>
